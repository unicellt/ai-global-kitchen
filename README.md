# AI环球厨房

AI环球厨房使用 Qwen Agent 流程理解自然语言中的食材、数量、菜系和口味，通过 D1 SQL 数据库严格匹配全球经典菜、融合创意菜、中餐经典和 AI 创意菜。数据库没有合适结果时，可生成经过食材白名单校验的创意菜谱。

## 核心规则

- 菜谱的全部必需食材必须出现在用户清单中。
- 只有油、盐、水被视为默认常备品。
- AI 负责意图理解和受约束创作，程序负责 SQL 工具调用和食材越界校验。

数据库包含 45 道菜谱、75 种食材和完整步骤，部署运行于 Cloudflare Workers 兼容环境。
